language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: "nosetests && sphinx-build doc doc/_build"

notifications:
  irc:
    channels:
      - "chat.freenode.net#icspy"
    use_notice: true
    skip_join: true